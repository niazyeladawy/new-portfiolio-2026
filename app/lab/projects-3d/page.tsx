'use client';

import ProjectsCarousel3D from '../../components/ProjectsCarousel3D';

/*
  Sandbox route for the WebGL projects drum. The production page still uses
  <StackCards />; nothing here is wired into it.
*/
const ProjectsLab = () => <ProjectsCarousel3D />;

export default ProjectsLab;
